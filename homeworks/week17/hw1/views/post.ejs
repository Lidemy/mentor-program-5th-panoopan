<% if(username){ %>

<!DOCTYPE html>
<html>
<head>
  <%- include("template/head")%>
</head>

<body> 
  <%- include("template/navbar") %> 

  <div class="col-10 col-md-6 my-5 mx-auto"  style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%)">
    <h1 class="text-center">POST</h1>
    <section class="error">
      <% if(errMessage && errMessage.length > 0) { %>
        <div class="alert alert-danger" role="alert">
          <%= errMessage %>
        </div>
      <% } %>
    </section>
    <form name="article" action="/post" method="POST" class="shadow-lg p-3 pb-5 mb-5 bg-white rounded">
      <div class="form-group">
        <input name="title" class="form-control" placeholder="請輸入文章標題"/>
      </div>
      <div class="form-group">
        <textarea name="content" rows="20" class="form-control" ></textarea>
      </div>
      <button type="submit" class="btn btn-primary float-right">Submit</button>
    </form>
  </div>

  <!--
  <div class="container-wrapper">
    <div class="container">
      <div class="edit-post">
        <form name="article" action="/post" method="POST">
          <div class="edit-post__title">
            發表文章：
          </div>
          <section class="error">
            <% if(errMessage) { %>
              <h3 class="error"><%= errMessage %></h3>  
            <% } %> 
          </section>
          <div class="edit-post__input-wrapper">
            <input name="title" class="edit-post__input"  placeholder="請輸入文章標題" />
          </div>
          <div class="edit-post__input-wrapper">
            <textarea name="content" rows="20" class="edit-post__content"></textarea>
          </div>
          <div class="edit-post__btn-wrapper">
            <input class="edit-post__btn" type="submit" value="送出">
          </div>
        </form>
      </div>
    </div>
  </div>  
-->
  <%- include("template/bundle") %> 
</body>
</html>

<% } %> 